name: macOS CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: macos-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      # Example: if you need dependencies (e.g. via Homebrew or other)
      - name: Install dependencies
        run: |
          brew update
          # brew install whatever-you-need
          # e.g. brew install cmake

      # Example: build / run tests (customise as needed)
      - name: Build / Run
        run: |
          # adjust commands to your project
          echo "Running on macOS CI"
          # e.g. make, cmake, python, etc.
