name: macOS CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: macos-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Install dependencies
        run: |
          brew update
          # Example: brew install cmake or python or any dependencies

      - name: Build / Run
        run: |
          echo "Running on GitHub-hosted macOS"
          pwd
          ls -l
          # Add your actual build or test commands here
